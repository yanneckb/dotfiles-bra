# Legacy terminal configs — kept for reference only.
# Primary terminal: Ghostty (`~/.config/ghostty/`)

## Kitty

Config: `kitty/kitty.conf` (Gruvbox Hard)

```sh
kitty --config ~/.config/legacy/kitty/kitty.conf
```

## iTerm2

Import themes from `iterm2/themes/` and profile from `iterm2/profiles/`.
